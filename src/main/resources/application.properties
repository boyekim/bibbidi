spring.application.name=bibbidi
spring.jpa.defer-datasource-initialization=true

# GPT API (client/gpt) - api key는 환경변수 OPENAI_API_KEY 로 주입
gpt.api-key=${OPENAI_API_KEY:}
gpt.base-url=https://api.openai.com/v1
gpt.model=gpt-4o-mini
gpt.connect-timeout=5s
gpt.read-timeout=60s
